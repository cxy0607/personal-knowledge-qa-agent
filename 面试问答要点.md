# 个人知识库 AI 问答工具（RAG + Agent）—— 面试问答要点

> 本项目是简历三个项目中最匹配「AI Agent 开发工程师」岗位的一个，Agent 相关的题面试官一定会深挖。

---

## 1. RAG 和 Agent 有什么区别？你这个项目为什么用 Agent？

**回答思路**（最重要的一题）：
- RAG 是**固定流水线**：检索 → 拼上下文 → 生成。路径确定、可控、成本低，适合"文档问答"
- Agent 是**模型自主决策**：模型根据问题自己决定用哪个工具、用几次、怎么用结果。灵活但成本高、可能失控
- 本项目是 Agent：问题来了模型先判断——知识库的问题走本地检索，时效性问题走联网搜索，检索不到会诚实说"没找到"
- 什么时候用 Agent：任务需要多步推理/多工具协作（比如"对比知识库里的旧数据和我联网搜到的新数据"）；简单问答用 RAG 更划算
- 我的客服系统项目就是 RAG（FAQ 优先 + 检索生成），两个项目正好对比

## 2. Agent 的工具调用原理是什么？（Function Calling）

**回答思路**：
1. 把工具的定义（名称、描述、参数 JSON Schema）随请求发给大模型
2. 模型决策需要工具时，返回一个"工具调用请求"（函数名 + 参数），而不是普通文本
3. 应用执行工具，把结果作为"工具消息"回传
4. 模型结合结果继续生成（可能需要再次调用工具，循环直到给出最终答案）
- LangChain 把这套循环封装成 create_agent + ReAct 模式：思考(Thought) → 行动(Action) → 观察(Observation)
- 前提：模型必须支持函数调用——本项目用 qwen3.8-27b（验证过函数调用能力）

## 3. ReAct 循环具体是什么？

**回答思路**：
- 论文《ReAct: Synergizing Reasoning and Acting in Language Models》的范式
- 每一步：模型先"思考"（该用什么工具）→ "行动"（发起工具调用）→ "观察"（读取工具返回）→ 再思考……直到能回答
- 实测例子（本项目）：问"北京今天天气"时，模型连续 3 次换关键词调 web_search（"北京今天天气 实时"→"北京天气 2025年 今天 气温 预报"→带引号精确搜索），最后诚实回答没搜到实时数据并给建议——这就是 ReAct 的多步尝试行为
- 对比：普通 Prompt 一次生成就结束，Agent 会自己迭代

## 4. 你的两个工具分别怎么实现的？

**回答思路**：
- search_knowledge_base：LangChain @tool 装饰的函数，内部调向量库相似度检索（top-4），结果格式化为"来源 + 相关度 + 内容"文本返回给模型。工具用工厂函数创建（闭包绑定当前选中的知识库 id——用户切换知识库后工具目标随之改变）
- web_search：requests 抓取 cn.bing.com 搜索结果页 + BeautifulSoup 解析标题/链接/摘要
- 为什么用 Bing 抓取：初版用 duckduckgo-search 库，国内网络不可达（超时/TLS 失败）——替换成 Bing 与项目网页导入同一套技术栈，零额外依赖（**选型被现实教育后调整的过程，是面试好故事**）

## 5. 工具失败了怎么办？Agent 会不会崩？

**回答思路**：
- 工具内 try/except：搜索失败返回"联网搜索暂时不可用"文本而不是抛异常——工具失败不炸整个 Agent
- 模型收到失败文本后会降级：基于已有知识回答，或告知用户稍后再试
- 对比：如果让异常冒泡，LangGraph 会中断整个执行，用户看到 500——降级设计是 Agent 工程化的关键

## 6. 切分策略为什么做了两种？

**回答思路**：
- 递归字符切分（500 字符/50 重叠）：适合 PDF、网页这类无标题结构的连续文本；按换行/句号等分隔符优先级递归切，尽量落在语义边界
- Markdown 结构切分：按 #/##/### 标题把文档切成"标题+正文"片段，片段自带章节上下文
- 为什么并存：定长切分"一刀切"会拆散 Markdown 的标题层级；结构切分对无标题 PDF 会退化成整篇一大块。**按文档类型自动选择，各取所长**
- 面试加分：能讲出"切分质量直接影响检索质量"——片段太大检索不精准，太小丢上下文

## 7. 相似度分数为什么会有负数问题？你怎么处理的？

**回答思路**（踩坑故事）：
- Chroma 默认用 l2 距离空间，`1 - l2距离` 会产生负"相似度"（实测 -0.08 这种反直觉分数）
- 修复：创建 collection 时指定 `collection_metadata={"hnsw:space": "cosine"}` 余弦空间，1 - 余弦距离 = 余弦相似度（0~1），分数语义直观（"66% 相关"）
- 这是我在客服系统项目里踩过的坑（langchain-chroma 的分数转换不生效），这次一开始就做对了

## 8. 为什么向量放 Chroma、元数据放 SQLite？

**回答思路**：两种数据的访问模式不同。
- 向量：需要语义相似度检索（ANN 近似最近邻），Chroma 专长，嵌入式部署零运维
- 元数据（有哪些文档、会话记录）：结构化查询（列表/筛选），SQLite 一行 SQL 搞定
- 各用最适合的存储；都持久化在项目目录，备份=拷走 data/ 文件夹
- 个人单用户工具用 SQLite 足够——选型匹配规模，不堆技术

## 9. 为什么自定义 Embeddings 类，不用 langchain-openai 现成的？

**回答思路**（真实踩坑，面试高分题）：
- langchain-openai 的 OpenAIEmbeddings 批量调用时用 tiktoken 把文本编码成 token 数组发给 API
- 百炼兼容接口只接受字符串/字符串列表（报 InvalidParameter: contents is neither str nor list of str）
- 解决：自定义类实现 LangChain Embeddings 协议（embed_documents/embed_query 两个方法），直接调百炼规范：字符串列表 + 每批最多 10 条
- 这个问题在我的客服系统项目里排查过（先看报错 → 最小复现 → 定位是请求体格式差异 → 自定义适配），这次直接复用解决方案

## 10. 网页导入怎么做正文提取的？

**回答思路**：
- requests 抓取（模拟浏览器 UA，部分站点拒绝默认 UA）→ 按 apparent_encoding 解码防乱码 → BeautifulSoup 丢弃 script/nav/header 等噪音标签 → get_text 提取正文 → 压缩连续空行
- 标题优先取 og:title 元信息（站点给搜索引擎/社交分享的标题，质量高于页面 title）
- 正文过短（<50 字符）判定导入失败并提示——过滤登录墙/纯 JS 渲染的页面
- 安全边界说明：个人本地工具未做 SSRF 防护，生产环境需要限制内网地址

## 11. Streamlit 的会话状态是怎么管理的？

**回答思路**：
- Streamlit 每次交互会重跑整个脚本——普通变量会丢，必须用 st.session_state 显式保存状态（当前知识库、会话 id、聊天记录）
- 持久化层用 SQLite：会话与消息落库，刷新页面/重启后历史可恢复
- 这是 Streamlit 与其他前端框架（React/Vue）最大的心智差异：**脚本式编程 + 显式状态管理**

## 12. 聊天历史的工具调用链是怎么实现可视化的？

**回答思路**：
- Agent 执行后从返回的消息序列提取：AI 消息里的 tool_calls（工具名+参数）与 ToolMessage（工具返回）按顺序组成 trace 列表
- trace JSON 化存入 SQLite 的 tool_calls 字段——会话恢复后依然能展开看到当时的思考过程
- 界面用 st.expander 折叠展示，每步标出"调用工具/工具返回"+ 内容
- 面试加分点：可观测性是 Agent 应用的关键——看不见决策过程就无法调试 Agent

## 13. 为什么模型要低温度，写作助手要高温度？

**回答思路**：按任务特性调温。
- Agent 决策要稳定：选哪个工具、传什么参数，随机性高了会乱调工具 → temperature 0.3
- 写作要多样性：同样输入希望不同输出 → 写作助手用 0.7
- 同一个 API 参数，不同任务不同取值——体现对模型行为的理解

## 14. 模型怎么选型？为什么用 qwen3.8-27b？

**回答思路**：
- 前提：Agent 必须函数调用能力（实测验证过）
- qwen-plus 免费额度用完 → 换 qwen3.8-27b（新模型有额度、支持函数调用、质量好）
- 模型名在 .env 配置，代码不写死；百炼是 OpenAI 兼容接口，换模型只改配置
- 展示的是"根据预算、能力、额度实际切换模型"的经验

## 15. 会话与消息的表结构怎么设计？

**回答思路**：
- sessions（会话标题/时间）+ messages（session_id 外键、role、content、tool_calls JSON、时间）
- 外键级联删除（PRAGMA foreign_keys ON）：删会话连带删消息
- tool_calls 用 TEXT 存 JSON：工具链是展示用数据，不需要按字段查询，JSON 字符串最简单
- 时间统一 UTC 存储（三个项目一致的约定）

## 16. 这个项目和你的客服系统都是 RAG，区别在哪？

**回答思路**：
- 客服系统：RAG 固定流水线 + FAQ 优先策略（向量匹配命中直接返回预设答案）——**可控、省钱**，适合企业客服场景
- 本项目：RAG 作为 Agent 的一个工具 + 联网搜索工具，模型自主决策——**灵活**，适合个人研究场景
- 技术栈对比：FastAPI+Vue3+MySQL（多用户企业系统）vs Streamlit+SQLite（个人工具）——按场景选架构
- 三个项目形成完整能力矩阵：RAG 系统 / 写作应用 / Agent 工具

## 17. 测试怎么做的？为什么全 mock？

**回答思路**：
- 16 个 pytest 用例：数据库 CRUD/级联删除、双切分策略、网页提取（含失败场景）、Bing 解析、知识库工具格式化、工具降级、trace 提取
- 原则：不调用真实外部服务（百炼 API/网络）——快速（3 秒）、稳定、零费用；真实调用在开发阶段手工验证
- 测试隔离细节：db.py 导入时绑定 DB_PATH，monkeypatch 必须同时 patch 使用方模块变量（这是踩过的坑）

## 18. 如果检索到的内容不相关怎么办？

**回答思路**：
- 相似度阈值过滤：低于阈值（如 0.3）的片段不进入上下文
- 工具返回"没有找到相关内容"，模型如实告知用户
- 系统提示词约束"基于检索内容作答，不编造"
- 进一步优化方向：Rerank（重排序）、混合检索（BM25 + 向量）、查询改写

## 19. Agent 的成本怎么控制？

**回答思路**：
- ReAct 循环每步都消耗 token（思考 + 工具调用 + 观察），成本高于单次生成
- 控制手段：限制最大循环步数；低温度减少无效重试；工具返回截断（检索 top-4、搜索 3 条、内容截 200 字符）
- 简单问题其实 RAG 就够——本项目是个人工具规模小，企业场景我会做"问题分类：简单问题走 RAG，复杂问题才进 Agent"（分层路由，业界常见做法）

## 20. 这个项目最难的地方是什么？

**回答思路**（自由发挥，讲真实故事）：
- 我的版本：联网搜索工具的选型波折——DuckDuckGo 库在国内不可达，排查了包改名（duckduckgo-search → ddgs）和 TLS 错误，最终换成 Bing 抓取并加上降级设计
- 加分结构：问题是什么 → 怎么排查的 → 怎么解决的 → 留下了什么（降级机制 + 可替换的搜索接口）
- 比"最难的是 XX 算法"更有说服力——真实踩坑过程能展示解决问题的思维方式
